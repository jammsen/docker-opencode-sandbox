# TEMPLATE — rendered by render-models into ~/.omp/agent/models.yml. `providers` is filled from
# the model catalog (~/.config/models/models.yml, managed by the model-config wizard): one provider
# per catalog server (keyed by server name), all its selected models, systemPromptSize 4096 each.
# Aggressive context trim: OMP refuses a request that would exceed contextWindow, so conservative
# maxTokens leaves headroom for the prompt.
providers: {}
